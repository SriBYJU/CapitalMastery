name: Apply course assessment state once

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply guarded course assessment state repair
        run: node tools/patch-course-assessment-state.mjs
      - name: Validate patched runtimes
        run: |
          node --check app.js
          node --check capital-mastery-live.js
          node tests/course-assessment-state-audit.mjs
          node tests/course-pass-continuity-audit.mjs
      - name: Commit repair
        run: |
          if git diff --quiet -- app.js capital-mastery-live.js; then
            echo 'No runtime changes were required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app.js capital-mastery-live.js
          git commit -m 'Centralize saved assessment progression and retakes'
          git push origin HEAD:main
