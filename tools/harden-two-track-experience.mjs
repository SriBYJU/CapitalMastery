import fs from 'node:fs';

const load=p=>fs.readFileSync(p,'utf8');
const save=(p,s)=>fs.writeFileSync(p,s);
function once(text,from,to,label){const i=text.indexOf(from);if(i<0)throw new Error(`Missing patch target: ${label}`);if(text.indexOf(from,i+from.length)>=0)throw new Error(`Ambiguous patch target: ${label}`);return text.slice(0,i)+to+text.slice(i+from.length);}
function regexOnce(text,re,to,label){const m=[...text.matchAll(new RegExp(re.source,re.flags.includes('g')?re.flags:re.flags+'g'))];if(m.length!==1)throw new Error(`${label}: expected one match, found ${m.length}`);return text.replace(re,to);}

// ------------------------------------------------------
// Learner-facing track semantics + self-directed routing
// ------------------------------------------------------
const trackPath='training-tracks.js';
let track=load(trackPath);
track=once(track,'verifiedCredentialCount: 3','verifiedCredentialCount: 4','Career Skills verified count');
track=once(track,"'Career Skills Program completion certificate'","'Career Skills Certificate'",'Career Skills milestone name');
track=once(track,"evidenceModel: '3 Standard 2.0 credentials + 1 evidence-backed program completion certificate'","evidenceModel: '4 verified credentials · Career Skills ends at the practical capstone'",'Career Skills evidence model');
track=track.replaceAll('${t.awardCount} awards','${t.verifiedCredentialCount} credentials');
track=once(track,"'Complete the core pathway and practical capstone. The fourth award is an evidence-backed program completion certificate; switch to Professional Readiness later without repeating completed stages.'","'Complete the core pathway and practical capstone to earn the Career Skills Certificate. Switch to Professional Readiness later without repeating completed stages.'",'Career Skills selected status');
track=once(track,"const pathwayRoutes = new Set(['career','learn','quiz','final']);","const pathwayRoutes = new Set(['career','learn','quiz','simulation','final','role-lab']);",'track route coverage');
track=once(track,"el.textContent = 'Career Skills: 4 awards · Professional Readiness: 5 credentials';","el.textContent = 'Career Skills: 4 credentials · Professional Readiness: 5 credentials';",'career directory counts');
track=once(track,"<p><strong>Career Skills</strong> is the shorter practical route with three Standard 2.0 credentials plus an evidence-backed completion certificate. <strong>Professional Readiness</strong> is the advanced route with all five career credentials, the full Role Lab and the flagship readiness credential. Work earned in the shorter route carries forward.</p>","<p><strong>Career Skills</strong> is the shorter practical route with four verified credentials: Foundations, Essentials, Applied Skills and the Career Skills Certificate. <strong>Professional Readiness</strong> is the advanced five-credential Standard 2.0 route with the full Role Lab and flagship readiness credential. Work earned in the shorter route carries forward.</p>",'learner guide hierarchy');
track=once(track,"<div><strong>4 awards</strong>3 credentials + completion certificate</div>","<div><strong>4 credentials</strong>Ends with Career Skills Certificate</div>",'learner guide Career Skills count');
track=once(track,"<p>Career Skills produces four learner-facing awards: Foundations, Essentials, Applied Skills and a Career Skills Program completion certificate backed by those credentials plus capstone evidence. Professional Readiness remains the five-level Standard 2.0 career credential stack: Foundations, Essentials, Applied Skills, Role Lab and Professional Readiness.</p>","<p>Career Skills produces four verified credentials: Foundations, Essentials, Applied Skills and the Career Skills Certificate, which requires the practical capstone simulation. Professional Readiness is the five-level Standard 2.0 career stack: Foundations, Essentials, Applied Skills, Role Lab and Professional Readiness. The two end credentials are intentionally different so a Career Skills completion is never mistaken for full Professional Readiness.</p>",'credentials page hierarchy');

const helperInsert=`  function apiPathway(careerId) {\n    return careerId === 'quant-finance' ? 'quantitative-finance' : careerId;\n  }\n\n  function publicPathway(pathwayId) {\n    return pathwayId === 'quantitative-finance' ? 'quant-finance' : pathwayId;\n  }\n\n  function v2AssessmentKey(careerId, stage) {\n    const id = apiPathway(careerId);\n    if (id === 'investment-banking') return stage === 'essentials' ? 'ib-essentials-case' : 'ib-professional-final';\n    return \`\${id}-\${stage === 'essentials' ? 'essentials-case' : 'professional-final'}\`;\n  }\n\n  function trackSequenceHtml(careerId, trackId) {\n    const id=encodeURIComponent(careerId), api=encodeURIComponent(apiPathway(careerId));\n    const essentials=encodeURIComponent(v2AssessmentKey(careerId,'essentials'));\n    const finalKey=encodeURIComponent(v2AssessmentKey(careerId,'final'));\n    const professional=trackId===PROFESSIONAL;\n    const steps=professional ? [\n      ['01','Foundations','Learn the role + technical core and earn Foundations',\`#/learn/\${id}/1\`],\n      ['02','Baseline','Measure starting readiness; this does not count against you',\`#/diagnostic/\${api}\`],\n      ['03','Essentials','Complete the secure mini case and earn Essentials',\`#/v2-assessment/\${essentials}\`],\n      ['04','Applied Skills','Complete Toolkit + Applied Work',\`#/learn/\${id}/3\`],\n      ['05','Role Lab','Perform the full professional workflow with review and revisions',\`#/role-lab/\${api}\`],\n      ['06','Professional Final','Clear the final calculation + judgment gate',\`#/v2-assessment/\${finalKey}\`],\n      ['07','Readiness','Review evidence coverage and professional readiness',\`#/readiness/\${api}\`]\n    ] : [\n      ['01','Foundations','Learn the role + technical core and earn Foundations',\`#/learn/\${id}/1\`],\n      ['02','Essentials','Apply the core concepts in the secure mini case',\`#/v2-assessment/\${essentials}\`],\n      ['03','Applied Skills','Complete Toolkit + Applied Work',\`#/learn/\${id}/3\`],\n      ['04','Career Skills Capstone','Complete the realistic job simulation and earn Career Skills',\`#/simulation/\${id}\`]\n    ];\n    return \`<section class="cm-track-sequence" data-cm-track-sequence><div class="cm-track-sequence-head"><div><div class="eyebrow">\${professional?'PROFESSIONAL READINESS SEQUENCE':'CAREER SKILLS SEQUENCE'}</div><h3>\${professional?'Five verified credentials. Full role-readiness evidence.':'Four verified credentials. Shorter, still practical.'}</h3></div><span>\${professional?'Advanced':'Shorter'}</span></div><div class="cm-track-sequence-grid">\${steps.map(([n,title,copy,href])=>\`<a href="\${href}"><b>\${n}</b><strong>\${title}</strong><span>\${copy}</span></a>\`).join('')}</div></section>\`;\n  }\n\n  function shapeCareerPath(root, careerId, trackId) {\n    const pathList=root.querySelector('.career-summary .path-list');\n    if(!pathList) return;\n    const headings=[...pathList.querySelectorAll('.path-step h3')];\n    const legacyFinal=headings.find(x=>/Professional Readiness Final/i.test(x.textContent||''))?.closest('.path-step');\n    const legacySimulation=headings.find(x=>/^Job Simulation$/i.test((x.textContent||'').trim()))?.closest('.path-step');\n    if(legacyFinal){ legacyFinal.hidden=true; legacyFinal.setAttribute('aria-hidden','true'); }\n    if(legacySimulation){\n      const hide=trackId===PROFESSIONAL;\n      legacySimulation.hidden=hide;\n      legacySimulation.setAttribute('aria-hidden',String(hide));\n    }\n    root.querySelector('[data-cm-track-sequence]')?.remove();\n    pathList.insertAdjacentHTML('afterend',trackSequenceHtml(careerId,trackId));\n  }\n\n  function advancedRouteCareer() {\n    const p=routeParts();\n    if(p[0]==='role-lab'&&p[1]) return publicPathway(decodeURIComponent(p[1]));\n    if(p[0]==='v2-assessment'&&p[1]&&/professional-final$/i.test(p[1])) {\n      const key=decodeURIComponent(p[1]);\n      if(key==='ib-professional-final') return 'investment-banking';\n      return publicPathway(key.replace(/-professional-final$/i,''));\n    }\n    return '';\n  }\n\n`;
track=once(track,'  function noticeHtml() {',helperInsert+'  function noticeHtml() {','track sequencing helpers');
track=once(track,'    updateFinalGate(root, careerId, track);\n    updateCapstoneBadge(root, track);','    updateFinalGate(root, careerId, track);\n    updateCapstoneBadge(root, track);\n    shapeCareerPath(root, careerId, track);','career track sequence render');

const oldGuard=`  function guardAdvancedRoute() {\n    const { route, pathwayId } = routeContext();\n    if (route !== 'final' || !pathwayId) return false;\n    if (getTrack(pathwayId) === PROFESSIONAL) return false;\n    sessionStorage.setItem(NOTICE_KEY, 'The Professional Readiness Final is part of the advanced program. Your Career Skills progress is preserved; upgrade when you want to continue.');\n    location.replace(\`#/career/\${encodeURIComponent(pathwayId)}\`);\n    return true;\n  }`;
const newGuard=`  function guardAdvancedRoute() {\n    const { route, pathwayId } = routeContext();\n    if(route==='final'&&pathwayId){\n      sessionStorage.setItem(NOTICE_KEY,'The legacy final is no longer a program gate. Career Skills ends at its capstone; Professional Readiness uses the Standard 2.0 Role Lab and Professional Final shown on the pathway.');\n      location.replace(\`#/career/\${encodeURIComponent(pathwayId)}\`); return true;\n    }\n    if(route==='simulation'&&pathwayId&&getTrack(pathwayId)===PROFESSIONAL){\n      sessionStorage.setItem(NOTICE_KEY,'Professional Readiness uses the deeper Role Lab instead of the shorter Career Skills capstone.');\n      location.replace(\`#/career/\${encodeURIComponent(pathwayId)}\`); return true;\n    }\n    const advancedCareer=advancedRouteCareer();\n    if(advancedCareer&&getTrack(advancedCareer)!==PROFESSIONAL){\n      sessionStorage.setItem(NOTICE_KEY,'Role Lab and the Professional Readiness Final are reserved for the advanced program. Your Career Skills work remains saved.');\n      location.replace(\`#/career/\${encodeURIComponent(advancedCareer)}\`); return true;\n    }\n    return false;\n  }`;
track=once(track,oldGuard,newGuard,'advanced-route guard');
save(trackPath,track);

let css=load('training-tracks.css');
if(!css.includes('.cm-track-sequence{')) css+=`\n.cm-track-sequence{margin:18px 0 0;padding:18px;border:1px solid #dce4ec;border-radius:16px;background:#fbfcfd}.cm-track-sequence-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:12px}.cm-track-sequence-head h3{margin:3px 0}.cm-track-sequence-head>span{border-radius:999px;background:#071a33;color:#fff;padding:6px 9px;font-size:.75rem;font-weight:800}.cm-track-sequence-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.cm-track-sequence-grid a{display:grid;grid-template-columns:auto 1fr;grid-template-areas:'n title' 'n copy';column-gap:10px;padding:12px;border:1px solid #e2e7ec;border-radius:12px;background:#fff;text-decoration:none}.cm-track-sequence-grid a>b{grid-area:n;color:#b28736}.cm-track-sequence-grid a>strong{grid-area:title;color:#071a33}.cm-track-sequence-grid a>span{grid-area:copy;color:#667487;font-size:.8rem;margin-top:2px}@media(max-width:700px){.cm-track-sequence-grid{grid-template-columns:1fr}.cm-track-sequence-head{flex-direction:column}}\n`;
save('training-tracks.css',css);

// ------------------------------------------------------
// Employer assignment/readiness semantics
// ------------------------------------------------------
const entPath='enterprise-v2.js';
let ent=load(entPath);
ent=once(ent,'const progress=assignedProgressSummary(report);','const progress=assignedProgressSummary(report,a.track); const programName=a.track===\'career_skills\'?\'Career Skills\':\'Professional Readiness\';\n      if(window.CM_TRAINING_TRACKS){const publicId=publicPathId(a.pathwayId);window.CM_TRAINING_TRACKS.setTrack(publicId,a.track===\'career_skills\'?window.CM_TRAINING_TRACKS.CAREER_SKILLS:window.CM_TRAINING_TRACKS.PROFESSIONAL);}','assigned track sync + progress');
ent=once(ent,'<span>professional gates complete</span>','<span>${a.track===\'career_skills\'?\'credential gates complete\':\'professional gates complete\'}</span>','assigned progress label');
ent=once(ent,"<strong>${progress.ready?'Professional Readiness verified':`${progress.pct}% through the evidence path`}</strong>","<strong>${progress.ready?`${programName} verified`:`${progress.pct}% through the ${a.track==='career_skills'?'Career Skills':'evidence'} path`}</strong>",'assigned completion label');
ent=regexOnce(ent,/  function assignedProgressSummary\(report=\{\}\) \{[\s\S]*?\n  \}/,`  function assignedProgressSummary(report={},track='professional') {\n    const creds=Array.isArray(report.credentials)?report.credentials:[];\n    const levels=new Set(creds.filter(c=>c.status==='active').map(c=>c.level));\n    if(track==='career_skills'){\n      const gates=[levels.has('foundations'),levels.has('essentials'),levels.has('applied'),levels.has('career')];\n      const complete=gates.filter(Boolean).length,total=gates.length;\n      return {complete,total,pct:Math.round((complete/total)*100),ready:levels.has('career')};\n    }\n    const gates=[levels.has('foundations'),!!report.diagnostic,levels.has('essentials'),levels.has('applied'),levels.has('role_lab')||report.roleLab?.status==='completed',!!report.finalAssessment?.passed];\n    const complete=gates.filter(Boolean).length,total=gates.length;\n    return {complete,total,pct:Math.round((complete/total)*100),ready:levels.has('professional_readiness')||report.readiness?.status==='ready'};\n  }`,'track-aware assigned progress');
ent=once(ent,'<div class="card cmv2-program-picker"><label>Assignment<select id="cmv2-report-picker">','<div class="card cmv2-program-picker"><label>Assignment<select id="cmv2-report-picker">','report picker anchor');
ent=once(ent,"</select></label></div><div class=\"cmv2-kpis cmv2-kpis-6\">","</select></label></div>${chosen.track==='career_skills'?`<div class=\"cmv2-warning-soft\"><b>Career Skills assignment</b><br>This shorter program is complete when the verified Career Skills Certificate is earned. Role Lab, Professional Final and Professional Readiness are intentionally not required.</div>`:''}<div class=\"cmv2-kpis cmv2-kpis-6\">",'Career Skills report scope note');
save(entPath,ent);

// ------------------------------------------------------
// Worker: portable Career Skills completion + clear title
// ------------------------------------------------------
const workerPath='v2/worker-v2-phase1-release.js';
let worker=load(workerPath);
worker=regexOnce(worker,/career:\s*`\$\{pathway\.role\}`\s*\+\s*`\$\{pathway\.track \? ` — \$\{pathway\.track\}` : ""\}`\s*\+\s*` Career Certificate`/,"career:\n      `${pathway.title} Career Skills Certificate`",'legacy Career Skills title');
worker=once(worker,"const readinessCredential=await env.DB.prepare(`SELECT credential_id,status,issued_at FROM credentials WHERE uid=? AND assignment_id=? AND credential_level='professional_readiness' ORDER BY issued_at DESC LIMIT 1`).bind(l.uid,a.id).first();","const targetCredential=a.credential_target==='career'\n              ? await env.DB.prepare(`SELECT credential_id,status,issued_at FROM credentials WHERE uid=? AND pathway_id=? AND credential_level='career' AND status='active' ORDER BY issued_at DESC LIMIT 1`).bind(l.uid,a.pathway_id).first()\n              : await env.DB.prepare(`SELECT credential_id,status,issued_at FROM credentials WHERE uid=? AND assignment_id=? AND credential_level='professional_readiness' ORDER BY issued_at DESC LIMIT 1`).bind(l.uid,a.id).first();",'employer target credential lookup');
worker=once(worker,"const complete=!!(readinessCredential&&readinessCredential.status==='active');","const complete=!!(targetCredential&&targetCredential.status==='active');",'target-aware completion');
worker=once(worker,"credential:readinessCredential?{credentialId:readinessCredential.credential_id,status:readinessCredential.status,issuedAt:readinessCredential.issued_at}:null","credential:targetCredential?{credentialId:targetCredential.credential_id,status:targetCredential.status,issuedAt:targetCredential.issued_at}:null",'target-aware report credential');
worker=once(worker,"reportAssignments.push({assignment:{id:a.id,cohortId:a.cohort_id,cohortName:a.cohort_name,pathwayId:a.pathway_id,credentialTarget:a.credential_target,status:a.status,dueAt:a.due_at,curriculumVersion:a.curriculum_version}","reportAssignments.push({assignment:{id:a.id,cohortId:a.cohort_id,cohortName:a.cohort_name,pathwayId:a.pathway_id,track:a.track,credentialTarget:a.credential_target,status:a.status,dueAt:a.due_at,curriculumVersion:a.curriculum_version}",'report assignment track');
save(workerPath,worker);

console.log('Two-track consistency hardening applied.');
